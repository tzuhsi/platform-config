replicaCount: 2

serviceAccount:
  create: true
  name: "etsy-sa"

image:
  repository: nginx
  pullPolicy: IfNotPresent
  tag: "1.25.3-alpine"

service:
  type: ClusterIP
  port: 80
  targetPort: 80

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi
